# clawdoc — Environment Variables
# clawdoc is a pure Bash tool with no external API dependencies.
# No runtime secrets are required.
#
# Optional overrides:
# CLAWDOC_LOG_DIR=~/.claude      # Directory containing Claude Code logs to analyze
# CLAWDOC_OUTPUT_DIR=./reports   # Where to write diagnostic reports
